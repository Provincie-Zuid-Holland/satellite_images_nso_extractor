[pytest]
markers =
    download: marks tests that require downloading satellite data (deselect with '-m "not download"')

# Default test configuration - skip download tests
addopts = -m "not download" --tb=short -v

# Test paths
testpaths = tests

# Minimum version
minversion = 6.0